<Window x:Class="GraphicEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GraphicEditor"
        mc:Ignorable="d"
        Title="Almost Paint" Height="800" Width="1300" Loaded="Window_Loaded">


    <Grid>
         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" HorizontalAlignment="Left" Margin="0,0,0,0.4" Width="140">
            <Button>Line</Button>
            <Button>Triangle</Button>
            <Button>Square</Button>
            <Button>Rectangle</Button>
            <Button>Circle</Button>
            <Button>Ellipse</Button>
        </StackPanel>
        <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Margin="0,0,0,0.4" Width="140">
            <Button>Cancel</Button>
            <Button>Clear</Button>
        </StackPanel>
        <Canvas Grid.Column="1" x:Name="CanvasMain" ClipToBounds="True">
        </Canvas>
    </Grid>
</Window>
